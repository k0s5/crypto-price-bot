{
  "name": "crypto-price-bot",
  "version": "1.0.0",
  "description": "",
  "author": "koss<jobs.koss@gmail.com>",
  "scripts": {
    "build": "npm run clean:build && tsc --build",
    "build:start": "npm run build && node dist/index.js",
    "clean:build": "rimraf dist tsconfig.tsbuildinfo",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "grammy": "^1.38.3",
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "pnpm": ">=10.14.0",
    "node": ">=22.20.0"
  }
}
